# Daily Cognition 内容与网页质量规范

这份文件是 `Zhao-wl/daily-cognition` 的长期发布基线。每日任务在生成新文章前应先读取本文件，并以仓库中现有长文作为实现参考。视觉表达的具体组件选择、改造优先级与实现建议同时参考 `VISUAL_GUIDE.md`。

## 1. 核心目标

这个项目不是摘要库、知识卡片库或 Word 文档归档，而是一套适合长期深度阅读的交互式跨学科知识网站。

网页化只改变呈现方式，不能降低正文深度。正文应至少达到此前聊天长文的完整度：通常 20–40 个递进小节，或达到等价信息量。不要为了“页面简洁”把一个主题压缩成 8–10 个每节两三句的短稿。

正文、图像、图表与交互共同承担解释职责。目标不是让页面“更丰富”，而是用最适合的媒介降低理解成本、建立心智模型，并保持长文的完整推理链。

## 2. 正文必须回答的问题

一篇完整文章通常需要覆盖：

- 它到底是什么，为什么值得理解；
- 起源与历史背景；
- 核心机制与必要的数学/模型；
- 关键人物、事件与思想演化；
- 重要分支、争议与学术边界；
- 常见误解及为什么会误解；
- 现实世界中的影响和案例；
- 与其他学科、制度、技术或思想的结构联系；
- 理解它以后，会改变怎样看问题。

正文必须讲清因果链和演化逻辑，不以罗列知识点代替解释。

## 3. 可视化原则

可视化必须帮助建立心智模型，而不是装饰。根据主题选择：

- 历史演化：时间轴、阶段切换、扩散地图；
- 机制：流程、反馈图、状态变化、分步示意或模拟；
- 概念体系：概念图、关系网络、层级结构；
- 对立理论：比较矩阵、二维立场图；
- 地理扩散：地图、路径图、区域变化；
- 数学关系：可调参数的图、函数曲线或数值实验；
- 空间结构：透视示意、剖面图、结构线、分层拆解；
- 实物/人物/地点：有可靠来源时使用真实图像，否则使用自包含 SVG/CSS 示意。

不要求每篇使用相同组件，也不要求为了“有图”而强行加入视觉元素。

### 3.1 视觉辅助是正文的一部分

凡是读者需要在脑中主动构建空间、关系、过程或变化的地方，都应进行一次“视觉表达检查”。尤其优先识别：

- 需要想象空间结构、三维关系或观察方向的段落；
- 存在多步机制、因果链、反馈回路或状态变化的段落；
- 存在复杂时间演化、人物谱系或制度演化的段落；
- 主要任务是比较多个理论、方案、阶段或制度的段落；
- 存在数学变量、概率、函数、尺度或参数变化的段落；
- 文字中反复出现“换句话说”“可以想象”“类似于”等提示，说明文字正在替读者承担高成本脑补的段落。

如果一张图能够显著降低理解成本，或替代数段高成本空间/结构说明，这张图就是正文，而不是配图。

### 3.2 每篇文章的视觉表达识别

完成文章结构后，必须额外扫描全文，识别最适合图像化表达的知识点。一般建议寻找 2–5 个候选点，但不设机械数量指标：简单主题可以只有 1 个高价值视觉模块，复杂主题可以更多。

对每个候选点判断：

1. 图像化是否比继续增加文字更容易理解；
2. 最适合的是静态示意、对比、时间轴、关系图、地图、数据图还是交互；
3. 图像本身是否能够回答一个明确问题；
4. 是否需要“怎么看这张图”的阅读提示；
5. 是否值得加入交互，还是静态图已经足够。

### 3.3 图像质量要求

重要图像应尽量自解释，并按需要包含：

- 明确标题；
- 关键标注；
- 图例或符号说明；
- 简短图注；
- 必要时的阅读顺序提示，如“先看……再看……最后比较……”。

如果一张图无法清楚说明问题，应拆分为多张步骤图或使用分步交互，而不是把过多标签堆到一个画面里。

SVG 应设置合适的 `viewBox` 并自适应容器；文字不能小到移动端无法阅读。图片必须有合适的 `alt` 或等价文字说明，不能让核心知识只存在于图像里。

### 3.4 图文组织方式

关键章节可以使用：

**核心结论 → 主图 → 怎么看这张图 → 深入解释 → 常见误解/边界**

复杂主题允许形成三层阅读：

- 一眼看懂层：主图 + 一句话结论；
- 理解层：图解 + 核心逻辑；
- 深入层：机制、数学、争议、历史与跨主题连接。

图像不应把长文变成卡片合集；正文仍承担完整解释，视觉模块负责帮助读者建立结构。

### 3.5 优先使用可维护的视觉资产

在没有必须使用照片或历史原图的情况下，优先使用：

- 自包含 SVG；
- HTML/CSS 几何示意；
- 少量原生 JavaScript 驱动的交互图；
- 浏览器原生可渲染的图表与结构图。

优先复用 `assets/style.css` 中的视觉容器和图注样式。复杂、文章专属的图形可以内联 SVG，但不要在每篇文章重复复制大段通用 CSS。

## 4. 交互必须有信息增量

禁止伪交互：

- 点击后只变颜色、描边、阴影，而内容不变；
- 时间轴点击后只重复按钮标题；
- 为了“看起来高级”给没有额外内容的概念强行加按钮。

合格交互至少完成一种事情：

- 展开显著的新解释；
- 切换原图 / 结构线 / 标注层；
- 切换对比视角；
- 改变参数并观察结果；
- 逐步展示因果链；
- 改变系统状态；
- 选择关系网络节点并解释关系；
- 在历史阶段之间切换并呈现背景、变化、意义和后续影响。

所有交互必须支持点击/触摸，不依赖 hover。

## 5. 跨主题连接是真实导航

如果连接到已经存在的主题，整张卡片必须是实际 `<a href>`，能够进入目标文章。禁止使用看起来像链接但没有 `href` 的普通 `<div>`。

同周使用同目录相对链接；跨周使用正确的 `../wXX/...` 路径。

卡片同时说明“为什么相关”，不能只有主题名。

## 6. 网站结构

稳定 URL：

`YYYY/wWW/YYYY-MM-DD-english-slug.html`

页面显示标题保持中文，真实 URL 使用稳定英文 slug，不使用中文路径、空格或高风险特殊字符。

每次发布应同步维护：

- 当周 `YYYY/wWW/index.html`；
- 必要时 `YYYY/index.html`；
- 必要时根 `index.html`；
- 上一篇 / 下一篇；
- 跨主题链接。

## 7. 样式与代码

优先复用 `assets/style.css`，避免每篇重复整套样式。文章专属交互可以有少量局部 CSS/JS。

不要为了统一模板而限制主题表达。一个数学主题和一个战争史主题可以拥有完全不同的可视化。

通用视觉模块应优先沉淀到共享样式中，例如：图像容器、图注、图例、步骤图、对比面板、阅读提示和移动端适配。文章内局部 CSS 只保留真正专属于该主题的部分。

## 8. 发布前 QA

每次发布至少检查：

1. 正文是否仍是完整长文，而非摘要；
2. 重要概念是否讲清原因、机制和后果；
3. 是否识别出最适合图像化表达的高认知负荷部分；
4. 可视化是否真的帮助理解，而不是装饰；
5. 重要图像是否具有必要标题、标注、图注或阅读提示；
6. 图像中的文字与结构在移动端是否仍清晰；
7. 核心知识是否同时有文字表达，不能只存在于图片中；
8. 每个交互点击后是否产生实质信息增量；
9. 跨主题卡片是否真实可跳转；
10. 上一篇/下一篇/周目录是否无断链；
11. CSS/JS/图片相对路径是否正确；
12. 桌面与移动端均可使用；
13. 页面不出现无意义横向滚动；
14. 新文章是否已成功提交到 `main`。

## 9. 旧文章持续改造

现有文章不是冻结资产。每周整理时应持续识别“可视化债务”，优先改造那些文字认知负荷高、图像化收益明显的页面。

旧文优先级综合考虑：

- 认知负荷；
- 图像化收益；
- 当前文字表达压力；
- 改造后组件的复用价值。

高优先级通常包括：空间/几何、机制/系统、历史演化、概念关系、数学/概率、技术架构等主题。

每次旧文改造以“显著降低理解成本”为目标，不要求整篇重写。可只改造 1–3 个最值得视觉化的小节，并确保不破坏原有正文、导航和链接。

## 10. 现有质量参考

现有文章应持续作为参考，但不是不可修改模板。尤其参考长文结构、交互信息增量和跨主题真实导航。新文章可以做得更好，不应机械复制旧页面。

视觉组件的选型、组件库与旧文改造评分方法见 `VISUAL_GUIDE.md`。
