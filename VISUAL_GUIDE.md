# Daily Cognition 可视化与旧文改造指南

本文件是 `CONTENT_GUIDE.md` 的视觉表达执行手册。总质量标准以 `CONTENT_GUIDE.md` 为准；本文件负责回答两个更具体的问题：**什么内容该用什么图**，以及**哪些旧文章应该先改**。

## 1. 基本原则

视觉表达不是“配图任务”，而是解释任务。每个视觉模块都应回答一个明确问题，例如：

- 结构到底长什么样？
- 这些元素之间是什么关系？
- 一个过程如何一步步发生？
- 两种理论真正区别在哪里？
- 参数改变后系统如何变化？
- 一个历史对象在不同阶段如何演化？

如果图片不能回答问题，只负责制造氛围，就不是高优先级视觉资产。

## 2. 视觉类型选择表

| 内容特征 | 首选视觉形式 | 典型例子 |
| --- | --- | --- |
| 空间、三维、观察方向 | 分步示意图、剖面图、结构线 | 透视、晶体、光学 |
| 时间演化 | 时间轴、阶段切换 | 技术史、战争、思想演化 |
| 因果和机制 | 流程图、反馈图、状态图 | 控制论、经济传导、生物机制 |
| 多概念关系 | 关系网络、层级图 | 哲学流派、制度构件、生态网络 |
| 多方案对比 | 比较矩阵、二维坐标 | 学派、技术方案、制度差异 |
| 地理扩散 | 地图、路径、区域分层 | 宗教传播、贸易、疾病扩散 |
| 数学关系 | 函数图、参数图、数值实验 | 贝叶斯、指数增长、供需 |
| 分类或命名体系 | 决策树、映射表 | R/S、分类学、协议层级 |
| 原图中存在隐藏结构 | 原图/标注图切换 | 建筑透视、艺术构图、战场地图 |

## 3. 第一批通用组件

### 3.1 `visual-figure`：主图容器

用于承载 SVG、图片、Canvas 或结构图。应包含标题和图注，并适配明暗主题与移动端。

### 3.2 `visual-reading`：怎么看这张图

重要图像旁给出 2–4 句阅读提示：先看什么、再比较什么、最终得到什么结论。它解决“有图但不会看”的问题。

### 3.3 分步示意图

适用于空间、几何、物理、结构机制。推荐 2–5 步；每一步新增一个结构层，而不是简单重复同一画面。

### 3.4 对比矩阵

适用于理论、制度、技术方案和分类差异。优先显示真正决定差异的维度，不要把普通表格包装成视觉组件。

### 3.5 时间轴 / 阶段切换

每个阶段至少提供背景、变化、意义、后续影响中的两项以上，禁止点击后只显示阶段标题。

### 3.6 流程图 / 因果链

节点要表示状态、动作或变量，边要表示明确关系，例如“导致”“反馈”“约束”“转换”。必要时按步骤高亮路径。

### 3.7 关系网络

点击节点后必须解释关系语义。不同边如果含义不同，需要图例区分，例如“继承”“反对”“影响”“同构”。

### 3.8 参数图 / 数值实验

滑块或输入改变后，图形与结论都应发生可观察变化。必须给出默认参数与合理范围，避免让读者面对没有解释的控制面板。

## 4. 图像的组织方式

关键章节优先使用：

1. 核心结论；
2. 主图；
3. “怎么看这张图”；
4. 深入解释；
5. 常见误解或边界。

对于单图信息过载的情况，改为：

- 原始场景；
- 加结构线；
- 加关键标注；
- 最终总结。

不要把十几个标签全部塞入第一张图。

## 5. SVG 实现规范

站点优先使用自包含 SVG 处理抽象结构和示意图：

- 必须设置 `viewBox`；
- 宽度使用响应式布局；
- 尽量复用 CSS 变量 `--text`、`--muted`、`--line`、`--accent`、`--accent2`、`--soft`；
- 避免固定浅色背景导致暗色主题失效；
- 文字字号在手机上仍需可读；
- 不依赖 SVG 内 hover 才能理解；
- 核心结论必须同时存在于正文或图注中。

## 6. 旧文章可视化债务评分

每篇旧文从四个维度各打 1–5 分：

| 指标 | 1 分 | 5 分 |
| --- | --- | --- |
| 认知负荷 | 纯叙事即可理解 | 需要强空间/机制/关系建模 |
| 图像化收益 | 有图只是更好看 | 有图能显著降低理解成本 |
| 当前表达压力 | 文字已经很清晰 | 大量脑补、重复解释、长段说明 |
| 组件复用价值 | 只适用于单篇 | 能沉淀为通用组件/范式 |

总分建议：

- 16–20：P0，优先立即改造；
- 12–15：P1，近期改造；
- 8–11：P2，排队处理；
- 4–7：P3，暂缓。

## 7. 当前文章类型的默认优先级

### P0 倾向

- 空间结构：透视、几何、光学、晶体、手性；
- 数学与概率：公式或参数关系直接决定理解的主题；
- 系统机制：反馈、控制、复杂循环；
- 技术架构：多个层级与数据流同时存在。

### P1 倾向

- 历史演化与思想史；
- 制度和经济机制；
- 多理论、多学派对比；
- 地理传播与战争过程。

纯人物叙事或概念已经非常直观的文章通常可以后置。

## 8. 每周整理时维护“可视化债务”

每周整理至少回答：

- 本周哪 1–3 篇文章最需要视觉补强？
- 每篇最值得改造的是哪个小节？
- 建议的视觉类型是什么？
- 是静态 SVG 就够，还是需要交互？
- 能否顺便沉淀一个以后复用的组件？

优先完成少量高价值改造，不追求一次性给所有文章加图。

## 9. 当前第一批改造策略

对仓库现有文章，第一批优先检查：

1. `Perspective`：空间投影、一点/两点/三点透视、投影公式；
2. `Chirality`：镜像不可重合、四面体碳、命名体系；
3. `Bayesianism`：先验—证据—后验的数值变化；
4. `Cybernetics`：反馈回路、控制器、目标与误差；
5. `Containerization`：进程/容器/虚拟机的边界与分层；
6. `Double-entry bookkeeping`：交易如何同时进入两个账户并保持恒等式。

实际改造顺序以每周扫描后的评分为准。
